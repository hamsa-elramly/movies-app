import React from 'react';
import { Typography } from '@mui/material';

const Header: React.FC = () => {
  return <Typography variant="h1">Movie Search Portal</Typography>;
};

export default Header;
